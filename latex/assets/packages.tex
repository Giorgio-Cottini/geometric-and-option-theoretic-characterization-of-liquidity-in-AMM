% assets/packages.tex
% !TEX root = ../main.tex

% ------------------------------------------------------------
% Language and quotation handling

\usepackage[english]{babel}
\usepackage{csquotes}

% ------------------------------------------------------------
% Page geometry

\usepackage[
    a4paper,
    top=3cm,
    bottom=3cm,
    inner=3.5cm,
    outer=2.5cm
]{geometry}

% ------------------------------------------------------------
% Fonts

\usepackage{newtxtext}

% ------------------------------------------------------------
% Mathematics and theorem environments

\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{newtxmath} % includes AMS symbol support; avoid loading amssymb separately

% ------------------------------------------------------------
% Spacing and indentation

\usepackage{setspace}
\setstretch{1.175}

\usepackage{indentfirst}

% ------------------------------------------------------------
% Headers and footers

\usepackage{fancyhdr}

% Configure later, for example:
% \pagestyle{fancy}
% \fancyhf{}
% ...

% ------------------------------------------------------------
% Section/title formatting

\usepackage{titlesec}

% ------------------------------------------------------------
% Lists

\usepackage{enumitem}

% ------------------------------------------------------------
% Tables and columns

\usepackage{multirow}
\usepackage{multicol}

% ------------------------------------------------------------
% Figures, captions, and plots

\usepackage{graphicx}
\usepackage{xcolor}

\usepackage[font=small, labelfont=bf]{caption}
\usepackage{subcaption}

\usepackage{float}

\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% ------------------------------------------------------------
% Code listings

\usepackage{listings}

\lstset{
    language=Python,
    basicstyle=\ttfamily\scriptsize,
    breaklines=true,
    breakatwhitespace=true,
    showstringspaces=false,
    numbers=none,
    keywordstyle=\color{blue},
    commentstyle=\color{teal},
    stringstyle=\color{red},
    frame=single,
    rulecolor=\color{black},
    tabsize=2,
    captionpos=b,
}

% ------------------------------------------------------------
% Bibliography

\usepackage[
    backend=biber,
    style=numeric,
    sorting=none,
    sortcites=true,
    doi=true,
    url=true,
    isbn=false,
    giveninits=true,
    maxbibnames=99
]{biblatex}

% Use this at the end of the document
% \printbibliography[heading=bibintoc]

% ------------------------------------------------------------
% Comments

\usepackage{comment}

% ------------------------------------------------------------
% Hyperlinks

\usepackage[
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    urlcolor=blue
]{hyperref}
